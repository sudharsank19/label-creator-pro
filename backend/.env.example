# SQLite database file location (relative to /prisma folder)
DATABASE_URL="file:./dev.db"

# Secret used to sign JWT tokens — change this in production
JWT_SECRET="label-creator-pro-super-secret-key-change-me-in-production"

# Port the API listens on
PORT=5000

